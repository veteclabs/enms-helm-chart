apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Namespace }}-{{ .Release.Name }}-test-connection"
  labels:
    {{- include "my-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Namespace }}-{{ .Release.Name }}:{{ .Values.service.port }}']
  restartPolicy: Never
